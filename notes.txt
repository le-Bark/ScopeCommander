compiler le ui
    pyuic5.exe '.\scopeCommanderMain window.ui' -o mainWindow.py
install requirements
    pip install -r requirements.txt
update requirements.txt
    pip freeze > requirements.txt

designer
    qt5-tools designer
    dans un shell sans le venv
    
packager en executable
    pyinstaller --onefile --hidden-import="pyvisa_py" --hidden-import="pyvisa-sim" .\main.py